#include <iostream>

/*
using namespace std;

int main() {

	// 赋值运算符 = 
	// 将等号右边的值，给左边的变量进行赋值。
	// 注意事项：在C++中，=是赋值的作用，不是用来判断两个数字是否相等的！
	int a = 10;
	a = 20;

	// 1. 赋值运算符，也是有运算结果的，运算的结果就是赋值完成之后的变量的值。
	cout << (a = 200) << endl;
	cout << a << endl;

	// 2. 组合运算符，将赋值运算符和其他的运算符组合在一起
	int num = 10;

	num += 10;		// 在现有值的基础上进行+10的操作，相当于 num = (int)(num + 10);
	num -= 10;		// num = (int)(num - 10);
	num *= 10;		// num = (int)(num * 10);
	num /= 10;		// num = (int)(num / 10);
	num %= 3;		// num = (int)(num % 3);


	char c = 'a';	// c = (char)(c + 2)
	cout << (c += 2) << endl;

	return 0;
}
*/